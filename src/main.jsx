import React from "react";
import ReactDOM from "react-dom/client";
import StrideApp from "./StrideApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StrideApp />
  </React.StrictMode>
);
